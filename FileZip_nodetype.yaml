node_types:
# File Zip task

  zipFile:
    derived_from: workflow_service
    properties:
      #input
      input-1:
        type: string
        default: file-wrapper
      #output:
      output-1:
        type: string
        default: file-wrapper

